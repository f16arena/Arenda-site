-- Аванс/переплата арендатора: нераспределённый остаток платежа после FIFO-гашения
-- начислений. Хранится на платеже и автоматически зачитывается в следующие
-- начисления (аудит 2026-06-10, п.5 — раньше остаток нигде не учитывался).
ALTER TABLE "payments" ADD COLUMN "unapplied_amount" DOUBLE PRECISION NOT NULL DEFAULT 0;
