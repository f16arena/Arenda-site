-- Государственный язык становится языком по умолчанию: новые пользователи
-- получают казахский интерфейс, русский переключается одним нажатием.
--
-- Уже заведённым пользователям язык НЕ меняем: они работают в системе прямо
-- сейчас, и молча переключить им интерфейс — хуже, чем оставить выбор за ними.

ALTER TABLE "users" ALTER COLUMN "locale" SET DEFAULT 'kk';
