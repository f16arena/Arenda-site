-- Условия договора по умолчанию для организации: город, день оплаты, пеня,
-- индексация, депозит, коммуналка, модули. Конструктор подставляет их в каждый
-- новый договор (условия из карточки арендатора важнее). Только добавление
-- nullable-колонки — существующие данные не трогаются.
ALTER TABLE "organizations" ADD COLUMN IF NOT EXISTS "contract_defaults" JSONB;
