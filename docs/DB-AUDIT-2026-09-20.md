# Аудит базы данных 20.09.2026

Проверено: доступ снаружи, изоляция организаций, целостность данных, схема Prisma,
индексы, миграции. База: Supabase Postgres 17 (Франкфурт), 67 таблиц, 40 МБ.

## Безопасность — чисто

- RLS включён на всех 67 таблицах; у ролей `anon` и `authenticated` нет прав
  (SELECT/INSERT/UPDATE/DELETE) — через публичный API Supabase данные не достать.
- Советник безопасности Supabase: 0 замечаний.
- Перекрёстных связей между организациями нет: арендаторы в чужих зданиях,
  помещения, документы, подписи, файлы, этажи целиком — по нулям.
- Пользователей без организации нет; арендаторов без пользователя нет.

## Исправлено 20.09

| Что | Как было | Сделано |
|---|---|---|
| Демо копило пользователей | 612 лишних строк: очистка сравнивала `email <> 'demo@…'`, а у демо-арендаторов почта пустая (в SQL такое условие NULL не выбирает) | условие переписано (`lib/demo.ts`), 606 строк удалены |
| Телеметрия росла без предела | web-vitals с мая, 14 600 строк — самая большая таблица | `lib/telemetry-retention.ts`: 30 дней для метрик, год для аудита; чистка в ночном кроне; 14 000 строк удалены |
| 13 связей без индексов | полный скан таблиц на проверках FK | индексы созданы, миграция `20260920120000_fk_indexes` |
| Дубль индекса документов | два одинаковых индекса | лишний удалён |
| Защита от двойного начисления | `charges_tenant_period_type_unique` жил только в ручном `migrations/020_*.sql`; в боевой базе есть, на новой базе не появился бы | добавлен в `prisma/migrations/20260920130000_indexes_and_guards` |
| Индексы под частые запросы | `@@index([deletedAt])` (двухзначное поле) бесполезен; хранилище без `deleted_at` в индексах | частичные индексы `WHERE deleted_at IS NULL` + составные (этажи по виду, уведомления, документы по номеру, аудит) |
| Удаление арендатора | документы, файлы, рассрочка оставались живыми; пользователь не скрывался | `lib/data/tenant-delete.ts` помечает их; старые записи поправлены в базе |
| Удаление тарифа | организация молча оставалась без тарифа | внешний ключ `ON DELETE RESTRICT` |

## Осталось — требует отдельной работы

**Высокий приоритет**
1. **Права ролей общие на платформу** (`role_permissions` без `organization_id`). Сейчас закрыто
   запретом: встроенные роли меняет только владелец платформы. Правильно — колонка организации
   и уникальность `(organization_id, role, section)`.
2. **Деньги хранятся как число с плавающей точкой** (`Float`) во всех финансовых полях. В коде уже
   есть ручное округление `round2()` — признак того, что копейки «плывут». Нужен тип `Decimal(14,2)`
   и сквозная правка финансового кода.
3. **19 таблиц не привязаны к организации** (документы, подписи, черновики, проекты конструктора,
   сессии, логи). При удалении организации останутся «висеть»; запрос «аудит по моей организации»
   не отвечается.
4. **Организацию и здание нельзя удалить**: каскад обрывается на этажах, помещениях, счётчиках,
   расходах. Либо каскады, либо явное «удаляем только мягко».

**Средний**
5. Нет уникальности номеров: договоры, документы, этажи, помещения, счётчики, показания. Плюс две
   параллельные схемы нумерации (`Organization.docNumberStart` и счётчики на здании) — обе без атомарности.
6. Файлы лежат в самой базе (`stored_files.data`, `generated_documents.file_bytes`,
   `document_templates.file_bytes`, `site_images.data`). У шаблонов нет ни ограничения размера, ни
   учёта в квоте, ни мягкого удаления. Правильно — объектное хранилище, в базе только ключ и хэш.
7. Даты без часового пояса (`timestamp`), а бизнес идёт по Алматы — границы «до какого числа» плывут.
8. Все перечисления — просто строки; значения только в комментариях.
9. Три системы миграций (`prisma/migrations`, `migrations/*.sql` вручную, `prisma/manual-migrations`) —
   отсюда и расхождение из пункта про двойное начисление. Свести к одной.

**Низкий**
10. JSON то в `Json`, то строкой; разбор через `JSON.parse` в try/catch без проверки схемы.
11. Нет `updatedAt` у начислений, оплат, договоров, зданий, этажей, помещений.
12. Легаси-поля: `BuildingDecor.onRoof`, `PaymentReport.receiptDataUrl`, `TenantDocument.fileUrl`.
13. 18 пустых таблиц: лиды (раздел удалён), счётчики, задачи, рассрочки, расходы, жалобы, API-ключи.

## Эксплуатация

- **Резервные копии не проверены** — на бесплатном тарифе Supabase их может не быть. Нужно
  настроить ежедневный дамп в отдельное хранилище.
- Подключения Auth ограничены 10 — при росте упрётся (советник Supabase).
