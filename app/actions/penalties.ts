"use server"

/**
 * УДАЛЕНО (см. AUDIT_2026-05-26.md, проблема #3).
 *
 * Ручная кнопка «Рассчитать пени» использовала формулу без PENALTY_GRACE_DAYS,
 * что приводило к рассинхрону с автоматическим cron-ом check-deadlines.
 *
 * Источник истины для пеней теперь — только cron app/api/cron/check-deadlines/route.ts.
 * Запускается ежедневно в 05:00 UTC.
 *
 * Stub оставлен на случай если функцию импортирует старый код — кидает явную
 * ошибку чтобы вызовы было легко найти.
 */
export async function calculatePenalties(): Promise<never> {
  throw new Error(
    "Ручной расчёт пеней удалён. Пени начисляются автоматически кроном " +
    "/api/cron/check-deadlines один раз в день. Если нужно вручную — " +
    "обратитесь к платформ-админу для разового запуска cron.",
  )
}
