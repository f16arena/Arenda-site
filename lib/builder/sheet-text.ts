// Переводчик для модулей, которые сами собирают текст на лист чертежа.
//
// Ключи вместо готовых строк (как в lib/system-health.ts) здесь не годятся:
// у одного узла до десяти выносок и пять строк состава конструкции — API
// превратился бы в список ключей длиннее самого расчёта. Лист печатается на
// языке того, кто его открыл, поэтому переводчик приходит из компонента листа.

import type { Messages } from "@/lib/i18n/messages"
import type { Translator } from "@/lib/i18n/translate"

export type SheetT = Translator<Messages>["t"]
